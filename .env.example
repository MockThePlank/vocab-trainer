# ==============================================
# Environment Configuration Template
# ==============================================
# Copy this file to .env and fill in your values
# DO NOT commit .env to version control!

# ==============================================
# Required Configuration
# ==============================================

# Admin API Key for protected routes (DELETE, PUT operations)
# Generate a strong random string (e.g., using: openssl rand -base64 32)
# This key must be sent in the X-API-Key header for admin operations
ADMIN_API_KEY=your-secret-admin-key-change-this-in-production

# ==============================================
# Optional Configuration
# ==============================================

# Application environment (development | production)
# Affects logging format and behavior
NODE_ENV=development

# Port the server listens on
# Default: 3000
PORT=3000

# Database path
# For local development: leave commented to use default (data/vocab.db)
# For Render.com deployment: set to /data/vocab.db (matches disk mount)
# DB_PATH=/data/vocab.db
